# Handoff: ChatGPT preview read confirmed; remaining pilot checks pending

Updated: 2026-09-05, prepared for commit review. The user explicitly approved preview deployment and live read testing. **Preview is deployed and enabled only for the verified owner account; production is unchanged.** Migration `0011_flimsy_mojo.sql` was applied to preview after recording a bookmark. The 35 implementation/documentation files are staged for the exact-tree approval gate; no commit or push has run at this checkpoint. After restarting the expired consent flow, the user confirmed ChatGPT successfully read their Workspace “Test 1” through WantKit Preview. This is a user-reported live result; browser control remains disconnected. Other tool reads and live disconnect/revocation have not been verified. Claude remains pending because the user signed in only to ChatGPT.

## Outcome and changed files

- Added ten read-only MCP tools over existing permission-checked services: Workspace, Collection, Item, comparison/Offers, saved research and text context. Reads preserve Collection-only membership and snapshot creator/export boundaries, exclude photo bytes and avatar URLs, and enforce schemas, paging, output bounds and rate limits.
- Added private OAuth registration, S256 authorization-code flow, explicit consent, rotating refresh tokens, per-user ownership, resource/scope checks, current login-session validation and disconnect. No anonymous registration or provider credential collection. New implementation: `apps/web/src/auth/mcp-options.ts` and `apps/web/src/worker/mcp-{auth-service,routes,server}.ts`.
- Added `ConnectorsPage.tsx`/CSS, app navigation and English/Persian copy; wired the maintained BetterAuth OAuth client. Updated auth configuration/schema, service read helpers, route/rate-limit integration, pinned dependencies and lockfile, test configuration and generated Worker types.
- New migration `apps/web/migrations/0011_flimsy_mojo.sql` adds seven OAuth tables and indexes only, with matching Drizzle metadata. It passed isolated tests and was subsequently applied to approved preview. It is unrelated to the archived unfinished API migration below.
- Updated roadmap/release notes and added [PRIVATE_MCP.md](./PRIVATE_MCP.md) with exact callback setup, account/privacy/cost boundaries, disconnect behavior and remaining live checks. Browser artifacts are ignored under `output/playwright/` and `.playwright-cli/`.
- At commit preparation, HEAD is `a96aa4e` on `main`. The 35 intended implementation/documentation files are staged for review; inspect Git for the current approval/commit state. The recovered API draft remains in its ignored local archive.

## Validation

- The user confirmed a successful live ChatGPT Workspace read (“Test 1”) after fresh consent. The reported assistant response also described the connector as read-only. This verifies the reported sign-in/read path, not every tool or live revocation; it does not claim independent browser observation.
- Full `bun run check` passed: lint, typecheck, Drizzle check, **140 tests** (26 domain, 5 localization, 80 existing Worker, 13 MCP, 16 UI-state), production build and Wrangler deployment dry-run. Log: `/tmp/wantkit-mcp-check.log`.
- MCP tests use the real local Worker/OAuth provider and isolated D1. They cover discovery/schemas/annotations, S256, consent denial, cross-user identity/registration boundaries, code and refresh replay, expiry/revocation, logout, allowlist removal, disconnect, audience/scope rejection, Collection-only and lost access, context privacy, paging, input/output limits, Origin checks and request limits.
- Local Playwright checks with fake session/connector responses passed for registration, secret reveal/dismissal, consent/return, provider callback selection and disconnect. English desktop and Persian 390-pixel layouts were visually inspected; browser console had zero errors/warnings. Screenshots: `output/playwright/mcp-consent-en.png`, `mcp-consent-fa-mobile.png`, `mcp-settings-fa-mobile.png`. These are mocked UI checks, not live provider validation.
- Cloudflare's `unhandled_rejection_after_microtask_checkpoint` flag fixes premature rejection reports during handled OAuth errors without changing the original compatibility date. A sandboxed test attempt could not bind localhost; the approved unrestricted full check passed. Build warnings for missing local secrets and a frontend chunk above 500 kB remain; that local check did not exercise live authentication.
- Baseline reconciliation previously restored source agreement with HEAD and passed all 127 existing tests before MCP work. Current active source now includes the new MCP changes; it no longer matches HEAD.

## Recoverable API draft — reference only

- Archive: [`.local-backups/openai-api-draft-2026-09-05.tgz`](./.local-backups/openai-api-draft-2026-09-05.tgz), mode `600`, local and untracked. It is not included in a clone or push; preserve it if moving this work to another machine.
- Archive SHA-256: `61b83dc7eca50b72a8eeef15667b031751c533b39cac3aa550015136e911dc8c`.
- Contents use original repository-relative paths plus `manifest.json` and `tracked-changes.patch`. The manifest records the full base commit and file hashes. Inspect or extract to a separate scratch directory; do not blindly restore it over active code.
- The archived migration selects new columns from old tables, attempts `PRAGMA foreign_keys=OFF`, and drops/rebuilds Research Runs and Requests with cascading dependents. Do not apply it. Any future provider migration needs aligned Drizzle metadata and a nonempty Sources/Results/Promotions preservation test.
- Current Cloudflare [migration guidance](https://developers.cloudflare.com/d1/reference/migrations/) and [foreign-key behavior](https://developers.cloudflare.com/d1/sql-api/foreign-keys/) were checked. D1 does not allow disabling foreign keys inside its implicit transactions; deferring validation is not a substitute for preserving dependent records.
- The draft's API model is not a local Codex model choice. No API adapter, budget service, route/workflow integration, UI, or tests were implemented by that draft.

## Next step — finish the already-approved live validation

1. ChatGPT sign-in and the first Workspace read are now confirmed by the user; finish the remaining live pilot checks, including disconnect/revocation. Reuse the existing private “WantKit Preview” plugin in the user's ChatGPT account, configured with `client_secret_post`, the displayed stable callback and `offline_access` base scope. No public listing. Browser control remains disconnected. The earlier aggregate counts (1 registration, 0 consents, 0 active tokens) predate successful consent and are no longer current verification. The original consent opened at 21:29 Amsterdam time, expired at 21:39 (`exp=1788637141`), and returned `invalid_signature` at 21:59. Installed BetterAuth checks confirmed that error also covers expiry. Restarting sign-in resolved it: the user reported ChatGPT could read Workspace “Test 1.” No code change or redeployment was needed. Preserve the working connection while arranging remaining tests; disconnect deletes its registration and requires new credentials to reconnect. Do not duplicate registrations or retrieve stored credentials.
2. Preview release `b51b0cd5-d575-4d39-8d8f-e24ef2232677` is at 100% traffic. Build/dry-run, deployment smoke, OAuth metadata and anonymous MCP boundary passed; preserved counts are 1 User, 1 Workspace, 1 Collection, 0 Items, 1 Image, zero FK violations. Bookmark/version/source hashes are in [RELEASE.md](./RELEASE.md). Checked-in flags remain disabled; the ignored generated deployment config contains the preview-only override. A rebuild resets it. Do not repeat migration or ask for the same deployment/live-test approval. Claude requires a signed-in account and remains unverified.
3. Commit/push preparation was requested after the successful ChatGPT read. Await explicit approval of the exact staged tree and proposed message, then recheck before committing. Do not commit the archive, credentials, account allowlist, logs or screenshots. This checkpoint does not authorize a new deployment.
4. Stop after the MCP pilot. Only after a new request proceed to the **private local Codex research runner**, then **AI Concept visualization**, then **web UI revamp**. Paid OpenAI API and Expo remain deferred. Tavily and Import Draft stay available.

## Workflow constraints

One substantial task per session; no subagents or parallel audits without explicit request. No commit, deployment, live allowance-consuming test or public endpoint/tunnel exposure without approval. Keep provider passwords, browser cookies, Codex auth files and subscription tokens out of the app and repository. A connector does not import all assistant memories and is neither offline nor unlimited free AI.

For any future commit, follow the user's complete staged-tree review gate and ask: “Proceed with this exact staged tree and commit message?” Wait for explicit approval, then recheck before committing. Never use blanket reset/restore/checkout/clean.
